[project]
name="cad-models"
version="0.0.0"
dependencies=[
    "bd_warehouse==0.2.0",
    "build123d==0.10.0",
    "cadquery-ocp",
    "click",
    "ocp_tessellate",
    "ocp_vscode",
]

[project.scripts]
cad-models = "cad_models.cli:main"